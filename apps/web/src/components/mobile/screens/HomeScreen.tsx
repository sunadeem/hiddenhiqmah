"use client";

import MobileHomeDashboard from "../MobileHomeDashboard";

/**
 * MobileHomeScreen — placeholder. Currently renders the dashboard only.
 *
 * Phase A-2 will redesign this as a true daily-companion screen:
 *   - Hero prayer card (next prayer big + countdown)
 *   - Today's verse + hadith
 *   - Streak counter + continue reading
 *   - Quick actions row
 *   - "Explore" link → MoreSheet
 */
export default function HomeScreen() {
  return <MobileHomeDashboard />;
}
