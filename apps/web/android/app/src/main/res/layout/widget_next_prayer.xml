<?xml version="1.0" encoding="utf-8"?>
<!-- Next-prayer widget.
     Deliberately plain RemoteViews: no Compose/Glance dependency for what is a
     label, two lines of text and a self-ticking countdown. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NEXT PRAYER"
        android:textColor="#D4A843"
        android:textSize="10sp"
        android:letterSpacing="0.14"
        android:maxLines="1" />

    <TextView
        android:id="@+id/widget_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Maghrib"
        android:textColor="#F1ECE0"
        android:textSize="24sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <Chronometer
        android:id="@+id/widget_countdown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#D4A843"
        android:textSize="20sp"
        android:maxLines="1" />

    <TextView
        android:id="@+id/widget_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#8D887C"
        android:textSize="13sp"
        android:maxLines="1" />

    <TextView
        android:id="@+id/widget_city"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#6B665C"
        android:textSize="11sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
